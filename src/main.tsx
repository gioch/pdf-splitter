import ReactDOM from "react-dom/client";
import App from "./App.tsx";

import "./index.css"; // Keep the global styles if needed

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />
);